backend: QuantizedXPU
cpp_namespace: at
class_name: AtenIpexTypeQuantizedXPU
use_out_as_primary: true
device_guard: true
supported:
  - _adaptive_avg_pool2d
#  - _empty_affine_quantized	# different signature
#  - adaptive_avg_pool2d        # should not register
#  - adaptive_avg_pool2d.out	# different signature
#  - add.Tensor	# different signature
#  - addmm	# different signature
  - as_strided
  - avg_pool2d
#  - avg_pool2d.out	# different signature
  - clone
  - copy_
  - dequantize.self
#  - empty.memory_format	# different signature
  - empty_strided
  - equal
#  - int_repr	# different signature
#  - leaky_relu	# different signature
#  - leaky_relu_	# different signature
  - max_pool2d_with_indices
  - q_per_channel_axis
  - q_per_channel_scales
  - q_per_channel_zero_points
  - q_scale
  - q_zero_point
  - qscheme
#  - quantize_per_tensor	# different signature
#  - quantized_max_pool2d	# different signature
  - record_stream
#  - resize_	# different signature
#  - set_quantizer_	# different schema
  - upsample_nearest2d
  - upsample_nearest2d.vec
  - view
